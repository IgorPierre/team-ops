{
  "name": "@team-ops/setup",
  "version": "0.1.0",
  "description": "Install the Team-Ops skill and MCP config for Cursor, Claude Code, or VS Code.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "team-ops-setup": "./dist/index.js"
  },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/team-ops/team-ops",
    "directory": "apps/setup"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node build.mjs",
    "prepack": "npm run build",
    "lint": "echo 'ok'",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "esbuild": "^0.25.8",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
